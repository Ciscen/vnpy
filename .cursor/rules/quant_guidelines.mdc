---
alwaysApply: true
---

# 量化开发与策略迭代规范 (Project-level Rules)

在进行量化策略开发、调参、回测和报告生成的过程中，所有的 AI Agent（包括 Cursor 和 Gemini）都必须严格遵守以下规范。

## 1. 统计陷阱与数据风控防范 (Pitfalls to Avoid)

在迭代策略时，必须时刻警惕并防范以下常见的量化陷阱：

- **幸存者偏差 (Survivorship Bias)**：
  - **规则**：绝不允许仅仅使用“当前”指数成分股（如当下的沪深300）去跑历史回测。
  - **强制措施**：任何选股域的过滤，必须采用 Point-in-Time (PIT) 历史快照数据（例如通过 BaoStock 获取每半年的成分快照并逐日映射），确保模型在 T 日只能看到 T 日及之前存在于该指数中的股票。
- **过拟合 (Overfitting) 与未来函数 (Look-Ahead Bias)**：
  - **规则**：禁止通过简单调高模型复杂度（如过大的 `max_depth`、过多的 `n_estimators`）或堆砌同质化因子来提高回测表现。
  - **强制措施**：特征构建和标签生成必须严格“后视”（Backward-looking），严禁使用包含未来信息的数据（如“全时段均值”、“前复权导致的未来信息泄漏”等须通过正确的框架接口处理）。
- **测试集调参 (Tuning on Test Set)**：
  - **规则**：严禁在样本外验证期（Out-of-Sample, OOS）进行反复网格搜索并挑选最好参数。
  - **强制措施**：所有的参数选择必须在样本内（In-Sample, IS）确定，然后才能放到独立的样本外窗口（如 `oos_validation`）进行“只读式”测试。不得以 OOS 结果反向修改策略参数。
- **熊市恶化与宏观分布冲突 (Bear Market Deterioration & Regime Conflict)**：
  - **规则**：不能仅凭牛市（或震荡向上）区间的优异表现就断定策略成功，必须经过完整的牛熊全周期（如 2022-2023 单边下行市场）的压力测试。
  - **强制措施**：如果策略在熊市发生严重回撤，且采用单一树模型进行标签混合（如牛市给A标签，熊市给B标签），容易导致模型特征分布混淆。解决宏观状态问题应优先使用**多模型融合（Ensemble）**机制，如分别训练 Beta 和 Alpha 模型，并在策略或执行层根据 Regime（如大盘 MA60）进行切换或加权。

## 2. 回测与仪表盘规范 (Dashboard Requirements)

所有产生的回测数据和前端仪表盘（如 `dashboard.html` / `strategy_overview.html`）必须满足以下要求：

- **基准对比 (HS300 Baseline)**：
  - 在计算超额收益、净值曲线时，必须引入“沪深300指数（000300.SSE）”等宽基指数作为基准线（Baseline）。
  - 图表（如净值图）中必须清晰画出策略净值与基准净值的对比曲线。
- **全周期展示 (Bull/Bear Periods)**：
  - 展示的数据必须涵盖完整的牛熊周期（例如 2022-2026），而不仅限于某一特定单边行情。
  - 核心指标表格中，除了总的 Sharpe、年化、最大回撤外，还需尽可能切分并体现不同宏观环境下的表现差异（如牛市的高弹性与熊市的抗跌性）。

## 3. 工作流：任务完成通知 (Feishu Notification)

- 每次策略迭代或核心任务执行完成后（包括遇到重大瓶颈需要中断并向用户提问时），必须通过飞书向用户发送执行结果或进度总结。
- **通知内容应包含**：
  - 当前任务的进展/策略优化点。
  - 核心的回测指标表现（总收益、年化、Sharpe、最大回撤）。
  - 遇到陷阱或问题的解释。
  - （可选但强烈推荐）调用飞书 API 附加可视化图表（如对比净值图）以便用户直观判断。
